buildscript {
    ext {
        kotlin_version = "1.7.10" // Or compatible version
    }
    dependencies {
        classpath("com.android.tools.build:gradle:7.3.0") // Or compatible version
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version")
    }
}